import {StyleSheet} from 'react-native';

function useStyleTranslationsList () {
   
    const styles = StyleSheet.create({
        list:{
            flex: 1,
        }
        
    });

    return styles;
}

export default useStyleTranslationsList;